{
  "name": "Lofoten coach-bot",
  "install": "sudo apt-get update -qq && sudo apt-get install -y -qq python3.12-venv && cd coach-bot && python3 -m venv .venv && ./.venv/bin/python -m pip install --upgrade pip -q && ./.venv/bin/pip install -q -e \".[dev]\""
}
